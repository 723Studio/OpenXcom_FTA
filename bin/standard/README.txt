This directory is the runtime location for official encrypted FTA content packages.

Expected generated packages include:

- From the Ashes.oxc
- HitFX-FtA.oxc

The .oxc files are generated build/release artifacts and must not be committed to Git unless the project policy explicitly changes.

Use tools/rpk-builder or the project packaging scripts to rebuild them from content/.
